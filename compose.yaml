name: signal-brief
services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=${POSTGRES_DB:-signal_brief}'
      - 'POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-123456}'
      - 'POSTGRES_USER=${POSTGRES_USER:-celestorng}'
    ports:
      - '5432'
